# data/urban/

暂无数据。

`docs/pipeline-map.md` 里 urban 场景的 `geometry`（建筑/道路网格）和 `layout`（建筑排列）两层目前都是 P0 缺口，`xasset-urban` 分支正在推进对应的 Stage 实现。等有 urban 场景的真实/合成样本数据（建筑轮廓、道路网络、建筑排布方案等）时，按 `data/house/` 的目录结构模式（`raw/` + `mapping/` + `converted/` + `convert.py`）在这里补充。
