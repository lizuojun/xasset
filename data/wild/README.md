# data/wild/

暂无数据。

`docs/pipeline-map.md` 里 wild 场景的 `geometry`（地形网格）和 `layout`（特征物散布）两层目前都是 P0 缺口，`xasset-outdoor` 分支正在推进对应的 Stage 实现。等有 wild 场景的真实/合成样本数据（地形/植被/水系分布等）时，按 `data/house/` 的目录结构模式（`raw/` + `mapping/` + `converted/` + `convert.py`）在这里补充。
